package com.example.vaultapp.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class VaultConfig {
    // Using Spring Cloud Vault auto-configuration
    // Configuration is handled through bootstrap.yml and application.yml
}