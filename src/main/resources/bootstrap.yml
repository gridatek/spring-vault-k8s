spring:
  application:
    name: vault-app
  cloud:
    vault:
      uri: http://vault:8200
      authentication: TOKEN
      token: myroot
      kv:
        enabled: true
        backend: secret
        profile-separator: '/'
        default-context: vault-app
      config:
        order: -10
      fail-fast: true
      timeout: 5s